import type { AppName } from "open" with { "resolution-mode": "import" };
export type Browser = {
    name: AppName;
    app: string;
};
export declare function getSupportedBrowsers(): Promise<Array<Browser>>;
export declare function getDefaultBrowser(): Promise<AppName>;
export declare const openUrlInBrowser: (url: string, queryParameters?: {
    [key: string]: string;
}) => Promise<void>;
