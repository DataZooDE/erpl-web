import { HTTPConfig, HTTPResponse } from "../../types";
export declare const DEFAULTS: {
    maxBodyLength: number;
    maxContentLength: number;
    maxRedirects: number;
};
export declare const fetch: (config: HTTPConfig) => Promise<HTTPResponse>;
